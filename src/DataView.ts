import { AxiomObject, KeyPair } from "axiom-api";

import VoteSet from "./VoteSet";

type ObjectMap = { [id: string]: AxiomObject };
type MapMap = { [parent: string]: ObjectMap };

export class DataView {
  // TODO: extract DataContextType
}
