{
  "name": "axboard",
  "version": "0.0.1",
  "private": true,
  "dependencies": {
    "axiom-api": "0.0.35",
    "react": "^16.8.6",
    "react-dom": "^16.8.6",
    "react-scripts": "3.0.1"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "deploy": "react-scripts build && gh-pages -d build -b master --repo git@github.com:axiom-org/axboard.github.io.git"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "homepage": "https://axboard.com",
  "devDependencies": {
    "@babel/runtime": "^7.5.4",
    "gh-pages": "^2.0.1"
  }
}
